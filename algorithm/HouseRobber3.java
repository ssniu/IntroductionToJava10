/**
 * The houses form a binary tree. If the root is robbed, its left and right can not be robbed.
 * Analysis
 * Traverse down the tree recursively. We can use an array to keep 2 values:
 * the maximum money when a root is selected and the maximum value when a root
 * if NOT selected.
 *
*/
